{
  "name": "poll-saas-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Poll SaaS MVP - poll function only (create, share, vote, results)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
